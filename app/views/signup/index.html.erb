<h1>Signup</h1>
<%= form_for @user, :url => { :action => 'submit' }, :html => { :multipart => true } do |form| %>
  <%= render 'signup/form', :form => form, :password => true %>
  <%= form.submit 'signup' %>
<% end %>
