<h1>Project#new</h1>
<%= form_for @project, :url => { :action => "create" }, :html => { :multipart => true } do |form| %>
  <%= render 'form', :form => form, :edit => false %>
  <%= form.submit 'submit' %>
<% end %>
